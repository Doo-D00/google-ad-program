# 설치 / 설정 가이드

**필요한 건 Gemini API 키 하나뿐입니다.** 한 번만 넣으면 글과 썸네일 모두 만들 수 있습니다.

---

## 0. 어디서 실행하나

| 방법 | 주소 |
|---|---|
| GitHub Pages | `https://dev-doo.github.io/google-ad-program/` |
| 로컬 | `http://localhost:8765` — `powershell -ExecutionPolicy Bypass -File dev-serve.ps1` |

`master` 에 푸시하면 1분 내로 반영됩니다.

> 이 리포는 **공개**입니다. 소스에는 키가 없습니다 — 사용자가 입력해 각자 브라우저의
> localStorage 에만 저장됩니다. **앞으로도 키를 코드에 넣지 마세요.**

> `file://` 로 열면 안 됩니다. ES 모듈이 동작하지 않습니다.

---

## 1. Gemini API 키 (이것만 하면 끝)

1. https://aistudio.google.com/app/apikey 접속
2. **API 키 만들기** (기존 키가 안 보이면 `프로젝트 가져오기` 로 프로젝트를 불러오거나
   그냥 새로 만드시면 됩니다. 무료입니다)
3. 앱 → **설정** → Gemini API 키에 붙여넣기 → **키 테스트** → 성공하면 자동 저장

모델은 `gemini-3.1-flash-lite`(기본) / `gemini-3.7-flash`(고품질이지만 자주 혼잡) /
`gemini-2.5-pro`(유료 한도 필요) 중에 고릅니다.

> 모델 ID는 자주 바뀝니다. 생성이 실패하면 `docs/js/gemini.js` 의 `TEXT_MODELS` 를
> [최신 문서](https://ai.google.dev/gemini-api/docs/models)의 모델로 갱신하세요.

---

## 사용 흐름

### 글쓰기 탭
1. **어떤 글을 쓸까요?** 에 키워드를 적고 분야를 고른 뒤 → **글 만들기**
   → 바로 올릴 수 있는 **완성본**이 나옵니다. 글은 워드처럼 화면에서 바로 고치시면 됩니다.
2. **링크 버튼** — 파란 버튼 자리에 문구와 주소를 넣거나, 글에서 원하는 위치를 클릭한 뒤
   직접 넣으세요. **개수 제한은 없습니다.** "제휴 링크 포함" 안내 문구는 자동으로 붙습니다.
3. **확인하기** — 길이·남은 자리·소제목·버튼·안내문구를 점검합니다.
   무엇이 몇 개 남았는지 이름으로 알려 줍니다. 알려만 주고 막지는 않습니다.
4. **미리보기** 탭에서 실제 발행 모습을 확인
5. **게시하기** → 글이 복사되고 티스토리 글쓰기 창이 열립니다.
   거기서 **Ctrl+V** 하시고 제목을 넣어 발행하세요.

> 설정에 블로그 주소(`myblog.tistory.com`)를 넣어 두셔야 글쓰기 창까지 열어 드립니다.
> 티스토리는 자동 발행이 불가능합니다 — 오픈 API가 2024년 2월에 종료됐습니다.

**빈칸 모드**: `내가 채울 빈칸을 남기기`를 체크하면 `[내 경험]` 같은 자리를 일부러 비워 둡니다.
노란 칸을 클릭하면 선택되니 바로 타이핑해서 채우시면 됩니다.
내 이야기가 들어간 글이 저품질을 피하는 데 유리합니다.

### 썸네일 탭
키워드를 적고 그림 풍(**실사 / 그림 / 애니메이션**)을 고른 뒤 → **썸네일 만들기**
→ **이미지 내려받기** → 티스토리 글쓰기에서 **대표 이미지**로 올리세요.

> 이미지는 무료 한도가 짧습니다. 연속으로 많이 만들면 잠시 기다려야 합니다.
> 기다리는 동안 남은 시간을 알려 드리고, 자동으로 이어서 진행합니다.

---

## 왜 빈칸을 남기나

이 도구는 완성된 글을 만들지 않습니다. **일부러 빈칸을 남깁니다.**

AI가 그대로 쓴 글을 대량 발행하면 애드센스 승인이 거절되고 트래픽도 떨어집니다.
티스토리는 저품질 판정에 특히 민감합니다. 사람의 경험·수치·판단이 들어간 글이라야 합니다.
빈칸을 채우는 것이 이 도구를 쓰는 핵심입니다.

금융 주제는 특히 조심하세요. 확인되지 않은 금액·금리·날짜는 `[실제 수치]` 로 비워 두고,
공식 출처로 확인한 뒤에 채우세요.
