# 티스토리 글 초안 도구 — 개발 지침

> 이 파일은 로컬 Claude Code가 이 프로젝트를 이어서 개발할 때 읽는 **컨텍스트/지침서**입니다.
> 원본 설계 문서(사용자 작성)를 이 프로젝트의 실제 제약에 맞춰 반영한 것입니다.
> 9번에 **원본과 다르게 간 부분과 그 이유**를 적어 두었습니다. 임의로 되돌리지 마세요.

## 1. 이 도구가 무엇인가
키워드를 넣으면 **애드센스 승인 요건을 갖춘 블로그 글 초안**을 만들고, 글 중간에
**제휴 링크 버튼**을 넣어 **HTML로 내보내는** 개인용 웹 도구.

- 사용자: 개발자 본인 1명 (다중 사용자·판매 없음)
- 발행 대상: **티스토리** — 기본 주소(.tistory.com)
- 콘텐츠 범위: 다양한 주제 (금융·창업·정보·생활·리뷰)
- 자동화 범위: **글 생성 + 버튼 삽입 + HTML 출력까지.** 발행은 사람이 직접 붙여넣는다.

**게시 자동화는 만들 수 없다.** 티스토리 오픈 API는 2024년 2월에 완전 종료되었고
글쓰기 API도 포함이다. 신규 발급도 불가능하다. 복사·붙여넣기가 유일한 경로다.

- 앱: `https://dev-doo.github.io/google-ad-program/` (Pages: `master` 브랜치 `/docs`)
- 자체 검증: `.../dev/selftest.html` (키 없이 순수 로직)
- 로컬: `powershell -ExecutionPolicy Bypass -File dev-serve.ps1` → `http://localhost:8765`
- **리포는 공개다. 키를 코드에 넣지 말 것.**

### 실사용 흐름 (이게 반드시 동작해야 한다)
1. 키워드 입력: "초기창업패키지 작성방법"
2. 초안 생성 (빈칸 포함)
3. 사람이 빈칸을 채우고 검토
4. 버튼 2~3개 삽입 (텍스트 + 제휴 URL)
5. 발행 전 점검
6. HTML 복사 → 티스토리 **HTML 모드**에 붙여넣고 게시

## 2. 가장 중요한 원칙 (이걸 어기면 도구의 존재 의미가 없다)
이 도구는 "완성된 글을 뱉는 기계"가 **아니다.**
**"승인 요건을 갖춘 초안 + 사람이 채울 빈칸을 남기는 조수"** 다.

2026년 3월 구글 코어 업데이트 이후 AI가 그대로 쓴 글을 대량 발행하면 애드센스 승인이
거절되고 트래픽도 급감한다. 사람이 경험·수치·판단을 더한 글만 정책 위반이 아니다.
**티스토리는 저품질 판정에 특히 민감하다.**

**절대 하지 말 것:** 글을 100% 완성해 그대로 발행 가능한 상태로 만드는 것.
남의 글을 베끼거나 흉내 내는 것.

> 프롬프트를 손볼 때 빈칸 4종과 골격을 빼지 말 것. selftest 가 이걸 검사한다.

## 3. 글 생성 규칙 (`gemini.js` 의 `buildPrompt`)
- 역할: "정보성 블로그 초안을 잡는 조수. 완성하지 말고 사람이 채울 빈칸을 남긴다."
- 빈칸 4종: `[내 경험]` `[실제 수치]` `[내 의견]` `[최신 확인]` — 각각 최소 한 번
- 복사 금지: 어떤 출처도 그대로 옮기지 말고 완전히 재구성
- 사실 원칙: 확인 안 된 수치·날짜·상품명·고유명사는 지어내지 말고 빈칸으로
- 분량: 빈칸 제외 본문 최소 1,000자
- 소제목 3개 이상
- 주제별 톤: 금융=신중·정확 / 창업·정보=실용 / 리뷰=경험 중심 / 생활=친근
- 출력: 첫 줄 `# 제목`, 이후 마크다운 본문

## 4. 글의 표준 골격
```
1 후킹  →  2 핵심정보A  →  3 [버튼1]  →  4 [내 경험]  →  5 핵심정보B/비교
→  6 [버튼2]  →  7 주의사항·팁([최신 확인])  →  8 (버튼3)  →  9 요약  →  10 제휴 고지
```
**버튼 배치 원칙**
- 글당 2~3개, 여러 지점에 분산
- **반드시 정보를 충분히 준 뒤에** 온다. 글 맨 앞에 두지 않는다.
- 각 버튼 바로 앞에 "왜 누르면 좋은지" 맥락 한 줄
- 생성 단계에서는 `[버튼: 텍스트 | URL]` 플레이스홀더로 두고, 사람이 채우면 실제 HTML로 변환

## 5. 버튼 HTML 사양 (`buttons.js`)
티스토리 HTML 모드는 **외부 CSS와 class를 유실시킨다.** 그래서 스타일을 전부 `style`
속성에 직접 박는다. **class 로 빼지 말 것** — 버튼이 맨 링크로 보인다.

- 파란 배경 `#2563eb` / 흰 글씨 / `border-radius:10px` / 가운데 정렬 / 은은한 그림자
- `rel="nofollow sponsored noopener"` **필수** — 제휴 링크는 구글 정책상 sponsored 표기 의무
- `target="_blank"` — 원 글 이탈 방지
- 버튼을 넣으면 **제휴 고지 문구가 자동으로 붙는다**(중복 추가 안 함)

## 6. 완성도 체크 (`checks.js`)
미흡이면 경고만 한다. **강제로 막지 않는다.** 판단은 사람이 한다.
- 본문 1,000자 이상 (태그·빈칸·버튼 자리를 뺀 순수 글자 수)
- 빈칸 0개 (버튼 자리 포함)
- 소제목 3개 이상
- 버튼 2개 이상이고 URL 채워짐
- 버튼이 있으면 제휴 고지 존재 (버튼이 없으면 해당 없음)

## 7. 출력 / 편집 방식

**사용자에게 HTML 을 보여주지 않는다.** 워드처럼 글 위에서 바로 고친다(`contenteditable`).
초기 버전은 HTML 을 textarea 에 넣어 편집하게 했는데 "일반 사람이 쓰기에 불편하다"는
피드백을 받고 갈아엎었다. **다시 HTML 편집기로 되돌리지 말 것.**

- 화면 문구는 전부 일상어로 쓴다. "플레이스홀더", "HTML 출력" 같은 말을 쓰지 않는다.
- 채울 빈칸은 노랗게(버튼 자리는 파랗게) 칠한다. 클릭하면 그 자리가 통째로 선택돼
  바로 덮어쓸 수 있다. 내용을 채우면 칠이 저절로 벗겨진다.
- **칠(`<mark class="blank">`)은 밖으로 나가면 안 된다.** 복사·저장·점검 전에 `unmarkBlanks`
  로 벗긴다. 안 벗기면 형광펜이 그어진 채로 티스토리에 발행된다.
- 붙여넣기는 서식을 버리고 글자만 받는다. 남의 사이트 스타일이 딸려오면 티스토리에서 깨진다.
- **복사는 서식이 살아 있는 상태로 한다**(`text/html` 클립보드). 그래서 티스토리 기본
  편집기에 그냥 Ctrl+V 해도 버튼과 소제목이 살아난다. HTML 모드로 들어갈 필요가 없다.
- `.html` 파일 보관 — 처음 만들어진 원본 마크다운을 주석으로 같이 넣는다.
- HTML 은 접어 둔 "HTML 보기"에서만 볼 수 있다. 읽기 전용이다.

## 8. 아키텍처
```
docs/index.html       화면 구조 + 설정 대화상자
docs/app.css          스타일 (좌 도구 패널 / 우 본문+미리보기, 각각 독립 스크롤)
docs/js/app.js        흐름 제어. 생성, 버튼 자리 UI, 점검, 복사/보관, 편집기 동작
docs/js/gemini.js     Gemini 호출 + 프롬프트(빈칸·골격). BLANKS 정의도 여기
docs/js/buttons.js    버튼 HTML, [버튼: …] 플레이스홀더 탐색/치환, 제휴 고지
docs/js/checks.js     발행 전 완성도 점검 (순수 함수 — selftest 로 검증)
docs/js/editor.js     빈칸 칠하기/벗기기 (화면 표시용)
docs/js/markdown.js   마크다운 → HTML, 첫 줄 "# 제목" 분리, esc/escAttr
docs/js/store.js      설정 저장 (localStorage). 키는 소스에 없다
dev-serve.ps1         docs/ 를 localhost:8765 로 띄우는 개발용 서버
```
**외부 의존성 0.** 순수 JS, 빌드 도구 없음, ES 모듈.

### Gemini API 레퍼런스
- POST `https://generativelanguage.googleapis.com/v1beta/models/<MODEL>:generateContent`
- 헤더 `x-goog-api-key`
- 바디: `{"systemInstruction":{"parts":[{"text":…}]},"contents":[{"role":"user","parts":[{"text":…}]}],`
  `"generationConfig":{"maxOutputTokens":16384}}`
- 응답: `candidates[0].content.parts[].text` (thought 파트 제외), `finishReason`,
  차단 시 `promptFeedback.blockReason`
- 모델(2026-08-19 무료 한도 실측): `gemini-3.1-flash-lite`(**기본** — 3~5초에 안정적) /
  `gemini-3.7-flash`(품질은 낫지만 503 과부하가 잦다) / `gemini-2.5-pro`(429, 무료로는 거의 못 씀)
- `maxOutputTokens` 는 넉넉히. thinking 이 출력 토큰을 나눠 쓰므로 작게 잡으면 본문이 빈다.
- **`systemInstruction` 을 거부하면 지시를 본문 앞에 붙여 한 번 재시도한다.**
- 503/500 과 초당 제한형 429 는 2초·5초로 두 번 재시도. **요금제 한도형 429 는 재시도하지
  않는다**(`isPlanQuota` 로 구분) — 기다려도 안 풀린다.
- CORS 는 브라우저에서 통과 확인됨. 프록시 불필요.

## 9. 원본 설계 문서와 다르게 간 부분 (이유 있음 — 되돌리지 말 것)
1. **Anthropic Messages API `claude-sonnet-4-6` → Gemini**
   사용자가 Anthropic API 키를 발급하지 못했다. 선택이 아니라 제약이다.
2. **localStorage 금지 → 사용함**
   원본의 금지 근거는 "지원 안 되는 환경 대비"인데, 이는 Claude.ai 아티팩트 환경의 제약이다.
   GitHub Pages 정적 페이지에는 해당하지 않는다. 금지하면 새로고침마다 키를 다시 넣고
   쓰던 글이 날아가 실사용이 불가능하다. 키는 소스에 없고 각자 브라우저에만 남는다.
3. **AI 썸네일 기능 제거**
   원본 문서에 없고, Gemini 이미지 모델은 무료 한도로 전부 429(요금제 한도)라 쓸 수 없었다.
4. **Blogger 게시 기능 제거**
   발행 대상이 티스토리로 바뀌었다. Cloud Console OAuth 설정은 더 이상 쓰지 않는다.

## 10. 알려진 함정 / 주의
- **`file://` 로 열면 안 된다.** ES 모듈이 동작하지 않는다. 반드시 http 로 띄운다.
- 상태 표시 헬퍼는 `className` 을 통째로 덮어쓰지 말 것 — 식별용 클래스가 날아가 다음
  호출에서 요소를 못 찾는다. `classList` 만 교체한다.
- 속성값(href 등)에는 `escAttr` 을 쓴다. `esc` 는 따옴표를 막지 않아 URL 하나로 태그가 깨진다.
- 자동 저장은 **빈 값으로 덮어쓰지 않는다.** 새 탭에서 열자마자 다른 탭 초안이 날아가기 때문.
  비우는 것은 [새 글] 버튼으로만.
- 미리보기 iframe 은 `sandbox=""` — 본문에 script 가 섞여도 실행되지 않는다.
- 금융(YMYL) 주제는 심사가 엄격하다. 금융 수치는 반드시 `[실제 수치]` 로 비운다.
  틀린 정보로 신청·구매를 유도하면 표시광고법 위반 및 계정 정지 위험.

## 11. 개발 순서 / 지금 상태
- [x] **1. MVP** — 키워드 → 빈칸 포함 초안 → 화면 표시
- [x] **2. 버튼 삽입 UI** — 플레이스홀더 자동 인식 + 텍스트/URL 입력 → 5장 HTML 변환
- [x] **3. 완성도 체크** — 5항목 판정 + 경고 표시
- [x] **4. HTML 복사 / .html 저장** — 원본 마크다운 주석 백업 포함
- [ ] **5. 생성 이력** — 만든 글 목록 보관, 다시 불러오기

**검증 상태(2026-08-19)**
- `dev/selftest.html` 71건 통과 — 프롬프트에 빈칸/골격 지시가 살아 있는지도 검사한다
- 버튼 자리 인식 → 변환 → 고지 자동 추가 → 점검까지 브라우저에서 확인
- 실제 Gemini 키로 초안 생성 확인

## 12. 코딩 규칙
- 순수 JS(빌드 도구 없음), ES 모듈, 한국어 주석 유지.
- 비밀정보(키) 커밋 금지. 전부 사용자 입력 → localStorage.
- 순수 로직은 모듈로 빼서 `selftest.html` 로 검증한다.

## 13. 레거시: 크롬 확장 (`src/`, `manifest.json`)
Blogger 편집기에 패널을 주입하던 v0.2~P2 버전. 사용 중단했다.
`docs/js/markdown.js` 는 이 코드에서 가져왔다. 지워도 된다.
